#include "date.h"

// on pourra utiliser le fichier date pour développer l'application.





// affiche à l'utilisateur la date 
/* void AfficherDate(tm date_anniversaire)
{
  struct tm date;
  printf("Votre date de naissaince est  %d %d / %d %d / %d %d %d %d\n ", date_anniversaire.nom);
 
} */

  //Saisie de la structure emission
/* void SaisirDate(tm *date_anniversaire)
{ 
  struct tm date;
  printf(" Saisir votre date de naissance au format jj/mm/aaaa", date_anniversaire.nom);
  scanf("%d %d / %d %d / %d %d %d %d", &date_anniversaire->tm_mday, &date_anniversaire->tm_mon+1, &date_anniversaire->tm_year+1900));
  */ //D est un pointeur donc fleche
//} 
